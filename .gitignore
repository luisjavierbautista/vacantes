# Guarda por si alguna vez vuelve el seguimiento de postulaciones: sería privado
# y no debe subirse nunca. Hoy la herramienta solo lista y cataloga.
postulaciones.json

# Caché de descargas y basura local
.cache/
__pycache__/
*.pyc
.DS_Store
